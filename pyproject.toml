[project]
name = "today"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
cli = ["timefhuman"]

[project.scripts]
diary = "today.cli:main"
kv_manager = "today.kv_cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
